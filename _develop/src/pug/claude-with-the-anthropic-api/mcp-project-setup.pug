extends ../utils/_lesson.pug

block append meta
  -
    var courseId = 'claude-with-the-anthropic-api'
    var currentSlug = 'mcp-project-setup'
    var lessonData = JSON.parse(nodeFs.readFileSync(nodePath.join(process.cwd(), '../data/claude-with-the-anthropic-api/mcp-project-setup.json'), 'utf8'))
    title = lessonData.titleEn + ' · Claude with the Anthropic API · Claude Academy'
    description = ''
    baseURL = 'https://chobadon.com/claude-academy/'
    ogImageURL = baseURL + 'assets/images/ogp.png'
    canonicalURL = `${baseURL}${courseId}/mcp-project-setup/`
