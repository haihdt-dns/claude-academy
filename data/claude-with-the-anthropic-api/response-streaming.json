{
  "slug": "response-streaming",
  "titleEn": "Response streaming",
  "titleVi": "Response streaming",
  "estimatedTimeEn": "8 minutes",
  "estimatedTimeVi": "8 phút",
  "prevLesson": "temperature",
  "nextLesson": "structured-data",
  "summaryEn": "<ul><li>Streaming lets you receive Claude's response token-by-token as it is generated, rather than waiting for the full response</li><li>Use <code>stream=True</code> (Python) to enable streaming; iterate over the returned event stream to process each chunk</li><li>Streaming dramatically improves perceived latency in chat UIs — users see output immediately instead of waiting</li><li>The SDK provides helper methods (<code>stream()</code>, <code>text_stream</code>) that handle the event loop for you</li></ul>",
  "summaryVi": "<ul><li>Streaming cho phép bạn nhận phản hồi của Claude từng token một khi nó được tạo ra, thay vì chờ toàn bộ phản hồi</li><li>Sử dụng <code>stream=True</code> (Python) để bật streaming; lặp qua luồng sự kiện được trả về để xử lý từng đoạn</li><li>Streaming cải thiện đáng kể độ trễ cảm nhận trong giao diện chat — người dùng thấy đầu ra ngay lập tức thay vì phải chờ</li><li>SDK cung cấp các phương thức trợ giúp (<code>stream()</code>, <code>text_stream</code>) xử lý vòng lặp sự kiện cho bạn</li></ul>",
  "supplementEn": "<h3>Why Streaming Matters</h3><p>By default, the Claude API waits until the entire response is generated before returning it. For short responses this is fine, but for longer outputs — detailed explanations, long-form writing, code — the user waits in silence until everything is ready.</p><p>Streaming solves this by sending tokens to your application as they are produced. The user sees text appearing word-by-word, just like typing. This makes your application feel significantly faster and more responsive, even if the total generation time is the same.</p><h3>How to Stream with the SDK</h3><p>The Anthropic Python SDK makes streaming straightforward. The recommended approach uses the <code>stream()</code> context manager:</p><pre><code>with client.messages.stream(\n    model=\"claude-opus-4-5\",\n    max_tokens=1024,\n    messages=[{\"role\": \"user\", \"content\": \"Write a short story.\"}]\n) as stream:\n    for text in stream.text_stream:\n        print(text, end=\"\", flush=True)</code></pre><p>The <code>text_stream</code> property yields each text delta as a string. You can also access the final complete message after the stream ends:</p><pre><code>    message = stream.get_final_message()\n    print(message.usage)  # token counts</code></pre><h3>Raw Event Streaming</h3><p>If you need lower-level control, you can iterate over the raw server-sent events:</p><pre><code>with client.messages.stream(...) as stream:\n    for event in stream:\n        print(event.type)  # e.g. content_block_delta, message_stop</code></pre><p>Each event has a <code>type</code> field. The most common ones are:</p><ul><li><code>content_block_delta</code> — carries a text chunk in <code>event.delta.text</code></li><li><code>message_stop</code> — signals the response is complete</li><li><code>message_delta</code> — carries final usage statistics</li></ul><p>For most applications, the <code>text_stream</code> helper is sufficient and much simpler to work with.</p>",
  "supplementVi": "<h3>Tại sao Streaming quan trọng</h3><p>Theo mặc định, Claude API chờ cho đến khi toàn bộ phản hồi được tạo ra trước khi trả về. Với các phản hồi ngắn điều này ổn, nhưng với đầu ra dài hơn — giải thích chi tiết, viết dài, code — người dùng chờ trong im lặng cho đến khi mọi thứ sẵn sàng.</p><p>Streaming giải quyết vấn đề này bằng cách gửi các token đến ứng dụng của bạn khi chúng được tạo ra. Người dùng thấy văn bản xuất hiện từng từ một, giống như gõ phím. Điều này khiến ứng dụng của bạn cảm thấy nhanh hơn đáng kể và phản hồi hơn, ngay cả khi tổng thời gian tạo giống nhau.</p><h3>Cách Stream với SDK</h3><p>Anthropic Python SDK làm cho streaming đơn giản. Cách tiếp cận được khuyến nghị sử dụng context manager <code>stream()</code>:</p><pre><code>with client.messages.stream(\n    model=\"claude-opus-4-5\",\n    max_tokens=1024,\n    messages=[{\"role\": \"user\", \"content\": \"Write a short story.\"}]\n) as stream:\n    for text in stream.text_stream:\n        print(text, end=\"\", flush=True)</code></pre><p>Thuộc tính <code>text_stream</code> trả ra từng delta văn bản dưới dạng chuỗi. Bạn cũng có thể truy cập message hoàn chỉnh cuối cùng sau khi stream kết thúc:</p><pre><code>    message = stream.get_final_message()\n    print(message.usage)  # số lượng token</code></pre><h3>Raw Event Streaming</h3><p>Nếu bạn cần kiểm soát cấp thấp hơn, bạn có thể lặp qua các raw server-sent event:</p><pre><code>with client.messages.stream(...) as stream:\n    for event in stream:\n        print(event.type)  # ví dụ: content_block_delta, message_stop</code></pre><p>Mỗi event có trường <code>type</code>. Những loại phổ biến nhất là:</p><ul><li><code>content_block_delta</code> — mang một đoạn văn bản trong <code>event.delta.text</code></li><li><code>message_stop</code> — báo hiệu phản hồi hoàn tất</li><li><code>message_delta</code> — mang thống kê sử dụng cuối cùng</li></ul><p>Với hầu hết các ứng dụng, helper <code>text_stream</code> là đủ và đơn giản hơn nhiều để làm việc.</p>",
  "sections": [
    {
      "id": "intro-video",
      "titleEn": "Response streaming",
      "titleVi": "Response streaming",
      "en": "",
      "vi": "",
      "image": "",
      "video": "<div class=\"lesson-video\"><iframe src=\"https://cdn.jwplayer.com/players/WyvqnwvX-Akyo8gQO.html\" frameborder=\"0\" allowfullscreen></iframe></div>"
    },
    {
      "id": "intro",
      "titleEn": "Introduction",
      "titleVi": "Giới thiệu",
      "en": "<p>When building chat applications with Claude, there's a significant user experience challenge: responses can take 10-30 seconds to generate, leaving users staring at a loading spinner. The solution is response streaming, which lets users see text appear chunk by chunk as Claude generates it, creating a much more responsive feel.</p>",
      "vi": "<p>Khi xây dựng ứng dụng chat với Claude, có một thách thức đáng kể về trải nghiệm người dùng: phản hồi có thể mất 10-30 giây để tạo ra, khiến người dùng nhìn chằm chằm vào vòng quay tải. Giải pháp là response streaming, cho phép người dùng thấy văn bản xuất hiện từng đoạn khi Claude tạo ra, tạo ra cảm giác phản hồi tốt hơn nhiều.</p>",
      "image": "<div class=\"lesson-image\"><img src=\"/claude-academy/assets/images/lesson/response-streaming-01.png\" alt=\"\"></div>",
      "video": ""
    },
    {
      "id": "problem-with-standard",
      "titleEn": "The Problem with Standard Requests",
      "titleVi": "Vấn đề với Request thông thường",
      "en": "<p>In a typical chat setup, your server sends a user message to Claude and waits for the complete response before sending anything back to the client. This creates an awkward delay where users have no feedback that anything is happening.</p>",
      "vi": "<p>Trong cài đặt chat thông thường, server của bạn gửi user message đến Claude và chờ phản hồi hoàn chỉnh trước khi gửi bất cứ thứ gì trở lại cho client. Điều này tạo ra độ trễ khó chịu khi người dùng không có phản hồi nào rằng có gì đang xảy ra.</p>",
      "image": "<div class=\"lesson-image\"><img src=\"/claude-academy/assets/images/lesson/response-streaming-02.png\" alt=\"\"></div>",
      "video": ""
    },
    {
      "id": "how-streaming-works",
      "titleEn": "How Streaming Works",
      "titleVi": "Streaming hoạt động như thế nào",
      "en": "<p>With streaming enabled, Claude immediately sends back an initial response indicating it has received your request and is starting to generate text. Then you receive a series of events, each containing a small piece of the overall response.</p><p>Your server can forward these text chunks to your client application as they arrive, allowing users to see the response building up word by word. All of these events are part of a single request to Claude.</p><div class=\"lesson-image\"><img src=\"/claude-academy/assets/images/lesson/response-streaming-04.png\" alt=\"\"></div>",
      "vi": "<p>Với streaming được bật, Claude ngay lập tức gửi lại phản hồi ban đầu cho biết nó đã nhận được yêu cầu của bạn và đang bắt đầu tạo văn bản. Sau đó bạn nhận được một loạt các sự kiện, mỗi sự kiện chứa một phần nhỏ của phản hồi tổng thể.</p><p>Server của bạn có thể chuyển tiếp các đoạn văn bản này đến ứng dụng client khi chúng đến, cho phép người dùng thấy phản hồi được xây dựng từng từ một. Tất cả những sự kiện này là một phần của một request đơn đến Claude.</p><div class=\"lesson-image\"><img src=\"/claude-academy/assets/images/lesson/response-streaming-04.png\" alt=\"\"></div>",
      "image": "<div class=\"lesson-image\"><img src=\"/claude-academy/assets/images/lesson/response-streaming-03.png\" alt=\"\"></div>",
      "video": ""
    },
    {
      "id": "stream-events",
      "titleEn": "Stream Events",
      "titleVi": "Các Stream Event",
      "en": "<p>When you enable streaming, Claude sends back several types of events:</p><ul><li><strong>MessageStart</strong> — A new message is being sent</li><li><strong>ContentBlockStart</strong> — Start of a new block containing text, tool use, or other content</li><li><strong>ContentBlockDelta</strong> — Chunks of the actual generated text</li><li><strong>ContentBlockStop</strong> — The current content block has been completed</li><li><strong>MessageDelta</strong> — The current message is complete</li><li><strong>MessageStop</strong> — End of information about the current message</li></ul><p>The <code>ContentBlockDelta</code> events contain the actual generated text that you'll want to display to users.</p>",
      "vi": "<p>Khi bạn bật streaming, Claude gửi lại một số loại sự kiện:</p><ul><li><strong>MessageStart</strong> — Một message mới đang được gửi</li><li><strong>ContentBlockStart</strong> — Bắt đầu một block mới chứa văn bản, tool use hoặc nội dung khác</li><li><strong>ContentBlockDelta</strong> — Các đoạn văn bản thực tế được tạo ra</li><li><strong>ContentBlockStop</strong> — Content block hiện tại đã hoàn thành</li><li><strong>MessageDelta</strong> — Message hiện tại đã hoàn chỉnh</li><li><strong>MessageStop</strong> — Kết thúc thông tin về message hiện tại</li></ul><p>Các sự kiện <code>ContentBlockDelta</code> chứa văn bản thực tế được tạo ra mà bạn sẽ muốn hiển thị cho người dùng.</p>",
      "image": "<div class=\"lesson-image\"><img src=\"/claude-academy/assets/images/lesson/response-streaming-05.png\" alt=\"\"></div>",
      "video": ""
    },
    {
      "id": "basic-implementation",
      "titleEn": "Basic Implementation",
      "titleVi": "Triển khai cơ bản",
      "en": "<p>To enable streaming, add <code>stream=True</code> to your messages.create call:</p><pre><code>messages = []\nadd_user_message(messages, \"Write a 1 sentence description of a fake database\")\n\nstream = client.messages.create(\n    model=model,\n    max_tokens=1000,\n    messages=messages,\n    stream=True\n)\n\nfor event in stream:\n    print(event)</code></pre><div class=\"lesson-image\"><img src=\"/claude-academy/assets/images/lesson/response-streaming-06.png\" alt=\"\"></div>",
      "vi": "<p>Để bật streaming, thêm <code>stream=True</code> vào lời gọi messages.create:</p><pre><code>messages = []\nadd_user_message(messages, \"Write a 1 sentence description of a fake database\")\n\nstream = client.messages.create(\n    model=model,\n    max_tokens=1000,\n    messages=messages,\n    stream=True\n)\n\nfor event in stream:\n    print(event)</code></pre><div class=\"lesson-image\"><img src=\"/claude-academy/assets/images/lesson/response-streaming-06.png\" alt=\"\"></div>",
      "image": "",
      "video": ""
    },
    {
      "id": "simplified-streaming",
      "titleEn": "Simplified Streaming",
      "titleVi": "Streaming đơn giản hóa",
      "en": "<p>Rather than manually parsing events, you can use the SDK's simplified streaming interface that extracts just the text content:</p><pre><code>with client.messages.stream(\n    model=model,\n    max_tokens=1000,\n    messages=messages\n) as stream:\n    for text in stream.text_stream:\n        print(text, end=\"\")</code></pre><p>This approach automatically filters out everything except the actual text content, which is usually what you need for displaying responses to users.</p>",
      "vi": "<p>Thay vì phân tích sự kiện thủ công, bạn có thể sử dụng giao diện streaming đơn giản hóa của SDK chỉ trích xuất nội dung văn bản:</p><pre><code>with client.messages.stream(\n    model=model,\n    max_tokens=1000,\n    messages=messages\n) as stream:\n    for text in stream.text_stream:\n        print(text, end=\"\")</code></pre><p>Cách này tự động lọc ra mọi thứ ngoại trừ nội dung văn bản thực tế, đây thường là những gì bạn cần để hiển thị phản hồi cho người dùng.</p>",
      "image": "",
      "video": ""
    },
    {
      "id": "getting-complete-message",
      "titleEn": "Getting the Complete Message",
      "titleVi": "Lấy Message hoàn chỉnh",
      "en": "<p>While streaming individual chunks is great for user experience, you often need the complete message for storage or further processing. After streaming completes, you can get the assembled final message:</p><pre><code>with client.messages.stream(\n    model=model,\n    max_tokens=1000,\n    messages=messages\n) as stream:\n    for text in stream.text_stream:\n        # Send each chunk to your client\n        pass\n    \n    # Get the complete message for database storage\n    final_message = stream.get_final_message()</code></pre><p>This gives you the best of both worlds: real-time streaming for users and a complete message object for your application logic.</p>",
      "vi": "<p>Trong khi streaming từng đoạn tuyệt vời cho trải nghiệm người dùng, bạn thường cần message hoàn chỉnh để lưu trữ hoặc xử lý tiếp. Sau khi streaming hoàn tất, bạn có thể lấy message cuối cùng được ghép lại:</p><pre><code>with client.messages.stream(\n    model=model,\n    max_tokens=1000,\n    messages=messages\n) as stream:\n    for text in stream.text_stream:\n        # Gửi từng đoạn đến client\n        pass\n    \n    # Lấy message hoàn chỉnh để lưu database\n    final_message = stream.get_final_message()</code></pre><p>Điều này cho bạn điều tốt nhất của cả hai thế giới: streaming thời gian thực cho người dùng và một message object hoàn chỉnh cho logic ứng dụng của bạn.</p>",
      "image": "",
      "video": ""
    }
  ]
}
