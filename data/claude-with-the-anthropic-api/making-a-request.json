{
  "slug": "making-a-request",
  "titleEn": "Making a request",
  "titleVi": "Thực hiện request",
  "estimatedTimeEn": "10 minutes",
  "estimatedTimeVi": "10 phút",
  "prevLesson": "getting-an-api-key",
  "nextLesson": "multi-turn-conversations",
  "summaryEn": "How to make your first API call to Claude — environment setup, request structure, and reading the response.",
  "summaryVi": "Cách thực hiện API call đầu tiên đến Claude — thiết lập môi trường, cấu trúc request và đọc response.",
  "supplementEn": "<h3>Your first request</h3><p>With your API key set as an environment variable, you're ready to make your first call. Here's the complete minimal example:</p><pre><code>import anthropic\n\nclient = anthropic.Anthropic()\n\nmessage = client.messages.create(\n    model=\"claude-3-5-sonnet-20241022\",\n    max_tokens=1024,\n    messages=[\n        {\"role\": \"user\", \"content\": \"What is the capital of France?\"}\n    ]\n)\n\nprint(message.content[0].text)</code></pre><h3>Request parameters</h3><h4>model</h4><p>The model ID to use. We recommend using a pinned version like <code>claude-3-5-sonnet-20241022</code> for predictable behavior. See the <a href=\"https://docs.anthropic.com/en/docs/about-claude/models\" target=\"_blank\" rel=\"noopener\">models documentation</a> for the full list.</p><h4>max_tokens</h4><p>The maximum number of tokens Claude can generate in its response. A token is roughly 3–4 characters. <code>1024</code> tokens is a good default for most short responses.</p><h4>messages</h4><p>An array of message objects. Each has a <code>role</code> (<code>\"user\"</code> or <code>\"assistant\"</code>) and <code>content</code> (the text). For a single-turn request, you only need one user message.</p><h3>Reading the response</h3><pre><code># The text of Claude's reply\nprint(message.content[0].text)\n\n# Why did Claude stop generating?\nprint(message.stop_reason)   # 'end_turn' or 'max_tokens'\n\n# Token usage for cost tracking\nprint(message.usage.input_tokens)\nprint(message.usage.output_tokens)</code></pre><p><strong>Why <code>content[0]</code>?</strong> The <code>content</code> field is a list because Claude can return multiple content blocks (text, tool calls, etc.). For a simple text response there will be exactly one block at index <code>0</code>.</p><p>If <code>stop_reason</code> is <code>\"max_tokens\"</code> rather than <code>\"end_turn\"</code>, Claude's response was cut off — increase <code>max_tokens</code> if you need the full reply.</p>",
  "supplementVi": "<h3>Request đầu tiên của bạn</h3><p>Với API key đã được đặt làm biến môi trường, bạn đã sẵn sàng thực hiện lần gọi đầu tiên. Đây là ví dụ tối giản hoàn chỉnh:</p><pre><code>import anthropic\n\nclient = anthropic.Anthropic()\n\nmessage = client.messages.create(\n    model=\"claude-3-5-sonnet-20241022\",\n    max_tokens=1024,\n    messages=[\n        {\"role\": \"user\", \"content\": \"What is the capital of France?\"}\n    ]\n)\n\nprint(message.content[0].text)</code></pre><h3>Các tham số request</h3><h4>model</h4><p>Model ID để sử dụng. Khuyến nghị dùng phiên bản đã ghim như <code>claude-3-5-sonnet-20241022</code> để có hành vi ổn định. Xem <a href=\"https://docs.anthropic.com/en/docs/about-claude/models\" target=\"_blank\" rel=\"noopener\">tài liệu về models</a> để biết danh sách đầy đủ.</p><h4>max_tokens</h4><p>Số token tối đa Claude có thể tạo. Một token xấp xỉ 3–4 ký tự. <code>1024</code> token là giá trị mặc định tốt cho hầu hết các response ngắn.</p><h4>messages</h4><p>Mảng các message object. Mỗi cái có <code>role</code> (<code>\"user\"</code> hoặc <code>\"assistant\"</code>) và <code>content</code> (text). Với request một lượt, bạn chỉ cần một user message.</p><h3>Đọc response</h3><pre><code># Text trong câu trả lời của Claude\nprint(message.content[0].text)\n\n# Tại sao Claude dừng tạo?\nprint(message.stop_reason)   # 'end_turn' hoặc 'max_tokens'\n\n# Số token để theo dõi chi phí\nprint(message.usage.input_tokens)\nprint(message.usage.output_tokens)</code></pre><p><strong>Tại sao lại <code>content[0]</code>?</strong> Trường <code>content</code> là một list vì Claude có thể trả về nhiều content block (text, tool call, v.v.). Với response text đơn giản sẽ có chính xác một block tại index <code>0</code>.</p><p>Nếu <code>stop_reason</code> là <code>\"max_tokens\"</code> thay vì <code>\"end_turn\"</code>, response của Claude đã bị cắt — hãy tăng <code>max_tokens</code> nếu bạn cần toàn bộ câu trả lời.</p>",
  "sections": [
    {
      "id": "intro-video",
      "titleEn": "",
      "titleVi": "",
      "en": "<p>Making your first request to the Anthropic API is straightforward once you understand the basic setup and structure. This guide walks through the essential steps to get Claude responding to your prompts using Python.</p>",
      "vi": "<p>Thực hiện request đầu tiên đến Anthropic API rất đơn giản khi bạn đã hiểu cấu trúc và thiết lập cơ bản. Hướng dẫn này đi qua các bước thiết yếu để Claude phản hồi các prompt của bạn bằng Python.</p>",
      "image": "",
      "video": "<div class=\"lesson-video\"><iframe src=\"https://cdn.jwplayer.com/players/LXX0R5AO-Akyo8gQO.html\" frameborder=\"0\" allowfullscreen></iframe></div>"
    },
    {
      "id": "setup",
      "titleEn": "Setting Up Your Environment",
      "titleVi": "Thiết lập môi trường",
      "en": "<p>Before making any API calls, you need to install the required packages and configure your API key securely.</p><p>First, install the necessary dependencies in your Jupyter notebook:</p><pre><code>%pip install anthropic python-dotenv</code></pre><p>Next, create a <code>.env</code> file in the same directory as your notebook to store your API key securely:</p><pre><code>ANTHROPIC_API_KEY=\"your-api-key-here\"</code></pre><p>This approach keeps your API key out of your code and prevents accidentally committing it to version control. Always add <code>.env</code> to your <code>.gitignore</code> file.</p><p>Load the environment variables and create your API client:</p><pre><code>from dotenv import load_dotenv\nload_dotenv()\n\nfrom anthropic import Anthropic\n\nclient = Anthropic()\nmodel = \"claude-sonnet-4-0\"</code></pre>",
      "vi": "<p>Trước khi thực hiện bất kỳ API call nào, bạn cần cài đặt các package cần thiết và cấu hình API key một cách an toàn.</p><p>Đầu tiên, cài đặt các dependency trong Jupyter notebook:</p><pre><code>%pip install anthropic python-dotenv</code></pre><p>Tiếp theo, tạo file <code>.env</code> trong cùng thư mục với notebook để lưu API key an toàn:</p><pre><code>ANTHROPIC_API_KEY=\"your-api-key-here\"</code></pre><p>Cách này giữ API key khỏi code và ngăn vô tình commit lên version control. Luôn thêm <code>.env</code> vào file <code>.gitignore</code>.</p><p>Load biến môi trường và tạo API client:</p><pre><code>from dotenv import load_dotenv\nload_dotenv()\n\nfrom anthropic import Anthropic\n\nclient = Anthropic()\nmodel = \"claude-sonnet-4-0\"</code></pre>",
      "image": "",
      "video": ""
    },
    {
      "id": "create-function",
      "titleEn": "The Create Function",
      "titleVi": "Hàm Create",
      "en": "<p>The core of making API requests is the <code>client.messages.create()</code> function. This function requires three key parameters:</p><div class=\"lesson-image\"><img src=\"/claude-academy/assets/images/lesson/making-a-request-001.png\" alt=\"create() function parameters\"></div><ul><li><strong>model</strong> — The name of the Claude model you want to use</li><li><strong>max_tokens</strong> — A safety limit on response length (not a target)</li><li><strong>messages</strong> — The conversation history you're sending to Claude</li></ul><p>The <code>max_tokens</code> parameter acts as a safety mechanism. If you set it to 1000, Claude will stop generating after 1000 tokens even if it has more to say. Claude doesn't try to reach this limit — it just writes what it thinks is appropriate and stops if it hits the maximum.</p>",
      "vi": "<p>Cốt lõi của việc thực hiện API request là hàm <code>client.messages.create()</code>. Hàm này yêu cầu ba tham số chính:</p><div class=\"lesson-image\"><img src=\"/claude-academy/assets/images/lesson/making-a-request-001.png\" alt=\"Các tham số hàm create()\"></div><ul><li><strong>model</strong> — Tên model Claude bạn muốn sử dụng</li><li><strong>max_tokens</strong> — Giới hạn an toàn về độ dài response (không phải mục tiêu)</li><li><strong>messages</strong> — Lịch sử hội thoại bạn gửi đến Claude</li></ul><p>Tham số <code>max_tokens</code> hoạt động như cơ chế an toàn. Nếu bạn đặt là 1000, Claude sẽ dừng tạo sau 1000 token dù có thể nói thêm. Claude không cố gắng đạt đến giới hạn này — nó chỉ viết những gì phù hợp và dừng lại khi đạt tối đa.</p>",
      "image": "",
      "video": ""
    },
    {
      "id": "messages",
      "titleEn": "Understanding Messages",
      "titleVi": "Hiểu về Messages",
      "en": "<p>Messages represent the conversation between you and Claude, similar to a chat application. There are two types of messages:</p><div class=\"lesson-image\"><img src=\"/claude-academy/assets/images/lesson/making-a-request-002.png\" alt=\"User and assistant messages\"></div><ul><li><strong>User messages</strong> — Content you want to send to Claude (written by humans)</li><li><strong>Assistant messages</strong> — Responses that Claude has generated</li></ul><p>Each message is a dictionary with a <code>role</code> (either <code>\"user\"</code> or <code>\"assistant\"</code>) and <code>content</code> (the actual text).</p>",
      "vi": "<p>Messages đại diện cho cuộc hội thoại giữa bạn và Claude, tương tự ứng dụng chat. Có hai loại message:</p><div class=\"lesson-image\"><img src=\"/claude-academy/assets/images/lesson/making-a-request-002.png\" alt=\"User và assistant message\"></div><ul><li><strong>User messages</strong> — Nội dung bạn muốn gửi đến Claude (do người viết)</li><li><strong>Assistant messages</strong> — Các response mà Claude đã tạo ra</li></ul><p>Mỗi message là một dictionary với <code>role</code> (là <code>\"user\"</code> hoặc <code>\"assistant\"</code>) và <code>content</code> (văn bản thực tế).</p>",
      "image": "",
      "video": ""
    },
    {
      "id": "first-request",
      "titleEn": "Making Your First Request",
      "titleVi": "Thực hiện Request đầu tiên",
      "en": "<p>Here's a complete example of making a request to Claude:</p><pre><code>message = client.messages.create(\n    model=model,\n    max_tokens=1000,\n    messages=[\n        {\n            \"role\": \"user\",\n            \"content\": \"What is quantum computing? Answer in one sentence\"\n        }\n    ]\n)</code></pre><p>When you run this code, Claude will process your request and return a response object containing the generated text along with metadata about the request.</p>",
      "vi": "<p>Đây là ví dụ hoàn chỉnh về thực hiện request đến Claude:</p><pre><code>message = client.messages.create(\n    model=model,\n    max_tokens=1000,\n    messages=[\n        {\n            \"role\": \"user\",\n            \"content\": \"What is quantum computing? Answer in one sentence\"\n        }\n    ]\n)</code></pre><p>Khi bạn chạy code này, Claude sẽ xử lý request và trả về response object chứa text đã tạo cùng với metadata về request.</p>",
      "image": "",
      "video": ""
    },
    {
      "id": "extract-response",
      "titleEn": "Extracting the Response",
      "titleVi": "Trích xuất Response",
      "en": "<p>The response object contains a lot of information, but you usually just want the generated text. Access it using:</p><pre><code>message.content[0].text</code></pre><p>This gives you clean, readable output like: <em>\"Quantum computing is a type of computation that leverages quantum mechanics principles like superposition and entanglement to process information using quantum bits (qubits), potentially solving certain complex problems exponentially faster than classical computers.\"</em></p><p>With these basics in place, you can start experimenting with different prompts and building more complex interactions with Claude.</p>",
      "vi": "<p>Response object chứa nhiều thông tin, nhưng thường bạn chỉ cần text đã tạo. Truy cập bằng:</p><pre><code>message.content[0].text</code></pre><p>Điều này cho bạn output rõ ràng, dễ đọc như: <em>\"Quantum computing là loại tính toán tận dụng các nguyên lý cơ học lượng tử như superposition và entanglement để xử lý thông tin bằng quantum bit (qubit), có thể giải quyết một số vấn đề phức tạp nhanh hơn theo cấp số mũ so với máy tính cổ điển.\"</em></p><p>Với những kiến thức cơ bản này, bạn có thể bắt đầu thử nghiệm với các prompt khác nhau và xây dựng các tương tác phức tạp hơn với Claude.</p>",
      "image": "",
      "video": ""
    }
  ]
}
