{
  "slug": "structured-data",
  "titleEn": "Structured data",
  "titleVi": "Dữ liệu có cấu trúc",
  "estimatedTimeEn": "10 minutes",
  "estimatedTimeVi": "10 phút",
  "prevLesson": "response-streaming",
  "nextLesson": "structured-data-exercise",
  "summaryEn": "<ul><li>Claude can return structured JSON output by instructing it via the system prompt or user message</li><li>Use Pydantic models or TypedDict to define the expected schema and validate Claude's response</li><li>Structured output is essential for pipelines where Claude's response feeds into downstream code</li><li>Always validate and handle parsing errors gracefully — Claude may occasionally deviate from the schema</li></ul>",
  "summaryVi": "<ul><li>Claude có thể trả về đầu ra JSON có cấu trúc bằng cách hướng dẫn qua system prompt hoặc user message</li><li>Sử dụng Pydantic model hoặc TypedDict để định nghĩa schema mong đợi và validate response của Claude</li><li>Đầu ra có cấu trúc rất cần thiết cho các pipeline mà response của Claude được đưa vào code phía sau</li><li>Luôn validate và xử lý lỗi parsing một cách khéo léo — Claude đôi khi có thể sai lệch so với schema</li></ul>",
  "supplementEn": "<h3>Why Structured Output?</h3><p>By default, Claude returns free-form text. This is great for conversations and summaries, but many real-world applications need Claude's output to feed directly into code — a database insert, an API call, a UI component.</p><p>For these use cases, you need <strong>structured output</strong>: a predictable, machine-readable format (typically JSON) that your application can parse and use without brittle string manipulation.</p><p>Common scenarios where structured output is essential:</p><ul><li>Extracting entities from a document (names, dates, amounts)</li><li>Classifying content and returning a category + confidence score</li><li>Populating a form or database record from unstructured input</li><li>Returning multiple fields in one API call (e.g. <code>summary</code>, <code>sentiment</code>, <code>tags</code>)</li></ul><h3>Prompting for JSON Output</h3><p>The simplest approach is to instruct Claude to respond with JSON in your system prompt, and specify the exact schema you expect:</p><pre><code>client.messages.create(\n    model=\"claude-opus-4-5\",\n    max_tokens=1024,\n    system=\"\"\"You are a data extraction assistant.\nAlways respond with a JSON object containing exactly these fields:\n- name (string)\n- date (string, ISO 8601 format)\n- amount (number)\nRespond with JSON only. No explanation.\"\"\",\n    messages=[\n        {\"role\": \"user\", \"content\": \"Invoice from Acme Corp on March 5 2024 for $1,250.\"}\n    ]\n)</code></pre><p>Tips for reliable JSON output:</p><ul><li>Say <strong>\"Respond with JSON only\"</strong> — this discourages preamble text</li><li>Define the schema explicitly, including field names and types</li><li>Use <code>\"No explanation\"</code> or <code>\"No markdown\"</code> to suppress code fences</li></ul><h3>Parsing and Validation</h3><p>Once Claude returns JSON, parse and validate it in your application. A robust pattern using Pydantic:</p><pre><code>import json\nfrom pydantic import BaseModel\n\nclass Invoice(BaseModel):\n    name: str\n    date: str\n    amount: float\n\nresponse_text = message.content[0].text\n\ntry:\n    data = json.loads(response_text)\n    invoice = Invoice(**data)\n    print(invoice.name, invoice.amount)\nexcept (json.JSONDecodeError, ValueError) as e:\n    print(f\"Parse error: {e}\")\n    # Retry or handle gracefully</code></pre><p>Pydantic automatically validates field types and raises a clear error if Claude returns unexpected data.</p>",
  "supplementVi": "<h3>Tại sao cần Structured Output?</h3><p>Theo mặc định, Claude trả về văn bản tự do. Điều này tốt cho hội thoại và tóm tắt, nhưng nhiều ứng dụng thực tế cần đầu ra của Claude đưa trực tiếp vào code — một lệnh insert database, một API call, một UI component.</p><p>Với những trường hợp này, bạn cần <strong>structured output</strong>: một định dạng có thể dự đoán, đọc được bởi máy (thường là JSON) mà ứng dụng của bạn có thể phân tích và sử dụng mà không cần thao tác chuỗi dễ gãy.</p><p>Các tình huống phổ biến mà structured output là thiết yếu:</p><ul><li>Trích xuất thực thể từ tài liệu (tên, ngày, số tiền)</li><li>Phân loại nội dung và trả về danh mục + điểm tin cậy</li><li>Điền vào form hoặc bản ghi database từ đầu vào phi cấu trúc</li><li>Trả về nhiều trường trong một API call (ví dụ: <code>summary</code>, <code>sentiment</code>, <code>tags</code>)</li></ul><h3>Prompt để nhận JSON Output</h3><p>Cách đơn giản nhất là hướng dẫn Claude phản hồi bằng JSON trong system prompt của bạn, và chỉ định schema chính xác bạn mong đợi:</p><pre><code>client.messages.create(\n    model=\"claude-opus-4-5\",\n    max_tokens=1024,\n    system=\"\"\"You are a data extraction assistant.\nAlways respond with a JSON object containing exactly these fields:\n- name (string)\n- date (string, ISO 8601 format)\n- amount (number)\nRespond with JSON only. No explanation.\"\"\",\n    messages=[\n        {\"role\": \"user\", \"content\": \"Invoice from Acme Corp on March 5 2024 for $1,250.\"}\n    ]\n)</code></pre><p>Mẹo để nhận JSON output đáng tin cậy:</p><ul><li>Nói <strong>\"Respond with JSON only\"</strong> — điều này ngăn văn bản mở đầu</li><li>Định nghĩa schema rõ ràng, bao gồm tên trường và kiểu dữ liệu</li><li>Dùng <code>\"No explanation\"</code> hoặc <code>\"No markdown\"</code> để tắt code fence</li></ul><h3>Parsing và Validation</h3><p>Sau khi Claude trả về JSON, phân tích và xác thực nó trong ứng dụng của bạn. Một pattern mạnh mẽ sử dụng Pydantic:</p><pre><code>import json\nfrom pydantic import BaseModel\n\nclass Invoice(BaseModel):\n    name: str\n    date: str\n    amount: float\n\nresponse_text = message.content[0].text\n\ntry:\n    data = json.loads(response_text)\n    invoice = Invoice(**data)\n    print(invoice.name, invoice.amount)\nexcept (json.JSONDecodeError, ValueError) as e:\n    print(f\"Parse error: {e}\")\n    # Thử lại hoặc xử lý khéo léo</code></pre><p>Pydantic tự động xác thực kiểu trường và đưa ra lỗi rõ ràng nếu Claude trả về dữ liệu không mong đợi.</p>",
  "sections": [
    {
      "id": "intro-video",
      "titleEn": "Structured data",
      "titleVi": "Dữ liệu có cấu trúc",
      "en": "",
      "vi": "",
      "image": "",
      "video": "<div class=\"lesson-video\"><iframe src=\"https://cdn.jwplayer.com/players/eNpA5r39-Akyo8gQO.html\" frameborder=\"0\" allowfullscreen></iframe></div>"
    },
    {
      "id": "the-problem",
      "titleEn": "The Problem",
      "titleVi": "Vấn đề",
      "en": "<p>When you need Claude to generate structured data like JSON, Python code, or bulleted lists, you'll often run into a common problem: Claude wants to be helpful and add explanatory text around your content. While this is usually great, sometimes you need just the raw data with nothing else.</p><p>Consider building a web app that generates AWS EventBridge rules. Users enter a description, click generate, and expect to see clean JSON they can immediately copy and use. If Claude returns the JSON wrapped in markdown code blocks with explanatory text, users can't simply copy the entire response — they have to manually select just the JSON portion.</p><p>By default, when you ask Claude to generate JSON, you might get something like this:</p><pre><code>```json\n{\n  \"source\": [\"aws.ec2\"],\n  \"detail-type\": [\"EC2 Instance State-change Notification\"],\n  \"detail\": {\n    \"state\": [\"running\"]\n  }\n}\n```\n\nThis rule captures EC2 instance state changes when instances start running.</code></pre><p>The JSON is correct, but it's wrapped in markdown formatting and includes explanatory text. For a web app where users need to copy the raw JSON, this creates friction in the user experience.</p>",
      "vi": "<p>Khi bạn cần Claude tạo dữ liệu có cấu trúc như JSON, code Python hoặc danh sách có dấu đầu dòng, bạn thường gặp phải một vấn đề phổ biến: Claude muốn hữu ích và thêm văn bản giải thích xung quanh nội dung của bạn. Mặc dù điều này thường tốt, đôi khi bạn chỉ cần dữ liệu thô không có gì khác.</p><p>Hãy xem xét việc xây dựng một web app tạo các AWS EventBridge rule. Người dùng nhập mô tả, nhấp tạo và mong đợi thấy JSON sạch mà họ có thể sao chép và sử dụng ngay. Nếu Claude trả về JSON được bọc trong các code block markdown với văn bản giải thích, người dùng không thể đơn giản sao chép toàn bộ phản hồi — họ phải chọn thủ công chỉ phần JSON.</p><p>Theo mặc định, khi bạn yêu cầu Claude tạo JSON, bạn có thể nhận được như sau:</p><pre><code>```json\n{\n  \"source\": [\"aws.ec2\"],\n  \"detail-type\": [\"EC2 Instance State-change Notification\"],\n  \"detail\": {\n    \"state\": [\"running\"]\n  }\n}\n```\n\nThis rule captures EC2 instance state changes when instances start running.</code></pre><p>JSON là đúng, nhưng nó được bọc trong định dạng markdown và bao gồm văn bản giải thích. Với web app nơi người dùng cần sao chép JSON thô, điều này tạo ra ma sát trong trải nghiệm người dùng.</p>",
      "image": "<div class=\"lesson-image\"><img src=\"/claude-academy/assets/images/lesson/structured-data-01.png\" alt=\"\"></div>",
      "video": ""
    },
    {
      "id": "solution",
      "titleEn": "The Solution: Prefilling and Stop Sequences",
      "titleVi": "Giải pháp: Prefilling và Stop Sequences",
      "en": "<p>You can combine assistant message prefilling with stop sequences to get exactly the content you want:</p><pre><code>messages = []\n\nadd_user_message(messages, \"Generate a very short event bridge rule as json\")\nadd_assistant_message(messages, \"```json\")\n\ntext = chat(messages, stop_sequences=[\"```\"])</code></pre><p>This technique works by:</p><ol><li>The user message tells Claude what to generate</li><li>The prefilled assistant message makes Claude think it already started a markdown code block</li><li>Claude continues by writing just the JSON content</li><li>When Claude tries to close the code block, the stop sequence immediately ends generation</li></ol><p>The result is clean JSON with no extra formatting:</p><pre><code>{\n  \"source\": [\"aws.ec2\"],\n  \"detail-type\": [\"EC2 Instance State-change Notification\"],\n  \"detail\": {\n    \"state\": [\"running\"]\n  }\n}</code></pre>",
      "vi": "<p>Bạn có thể kết hợp prefilling message trợ lý với stop sequence để nhận chính xác nội dung bạn muốn:</p><pre><code>messages = []\n\nadd_user_message(messages, \"Generate a very short event bridge rule as json\")\nadd_assistant_message(messages, \"```json\")\n\ntext = chat(messages, stop_sequences=[\"```\"])</code></pre><p>Kỹ thuật này hoạt động bằng cách:</p><ol><li>User message cho Claude biết phải tạo gì</li><li>Message trợ lý được điền trước khiến Claude nghĩ rằng nó đã bắt đầu một markdown code block</li><li>Claude tiếp tục bằng cách chỉ viết nội dung JSON</li><li>Khi Claude cố gắng đóng code block, stop sequence ngay lập tức kết thúc quá trình tạo</li></ol><p>Kết quả là JSON sạch không có định dạng thêm:</p><pre><code>{\n  \"source\": [\"aws.ec2\"],\n  \"detail-type\": [\"EC2 Instance State-change Notification\"],\n  \"detail\": {\n    \"state\": [\"running\"]\n  }\n}</code></pre>",
      "image": "<div class=\"lesson-image\"><img src=\"/claude-academy/assets/images/lesson/structured-data-02.png\" alt=\"\"></div>",
      "video": ""
    },
    {
      "id": "processing-and-beyond",
      "titleEn": "Processing and Beyond",
      "titleVi": "Xử lý và mở rộng",
      "en": "<p>You might notice some extra newline characters in the response. These are easy to handle:</p><pre><code>import json\n\n# Clean up and parse the JSON\nclean_json = json.loads(text.strip())</code></pre><p>This technique isn't limited to JSON generation. Use it anytime you need structured data without commentary:</p><ul><li>Python code snippets</li><li>Bulleted lists</li><li>CSV data</li><li>Any formatted content where you want just the content, not explanations</li></ul><p>The key is identifying what Claude naturally wants to wrap your content in, then using that as your prefill and stop sequence. This approach gives you precise control over Claude's output format, making it much easier to integrate AI-generated content into applications where clean, structured data is essential.</p>",
      "vi": "<p>Bạn có thể nhận thấy một số ký tự xuống dòng thêm trong phản hồi. Chúng dễ xử lý:</p><pre><code>import json\n\n# Làm sạch và phân tích JSON\nclean_json = json.loads(text.strip())</code></pre><p>Kỹ thuật này không giới hạn ở việc tạo JSON. Sử dụng nó bất cứ khi nào bạn cần dữ liệu có cấu trúc mà không có bình luận:</p><ul><li>Các đoạn code Python</li><li>Danh sách có dấu đầu dòng</li><li>Dữ liệu CSV</li><li>Bất kỳ nội dung có định dạng nào mà bạn chỉ muốn nội dung, không phải giải thích</li></ul><p>Điều quan trọng là xác định những gì Claude tự nhiên muốn bọc nội dung của bạn, sau đó sử dụng điều đó làm prefill và stop sequence của bạn. Cách tiếp cận này cho bạn kiểm soát chính xác định dạng đầu ra của Claude, giúp dễ dàng tích hợp nội dung do AI tạo ra vào các ứng dụng nơi dữ liệu sạch, có cấu trúc là thiết yếu.</p>",
      "image": "",
      "video": ""
    }
  ]
}
